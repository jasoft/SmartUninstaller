<UserControl x:Class="SmartUninstaller.UI.Views.SystemCleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SmartUninstaller.UI.Converters">
    <UserControl.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="系统清理" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="🔍 分析系统" Command="{Binding AnalyzeCommand}" Margin="20,0,0,0" Padding="10,5" Cursor="Hand"/>
            <ProgressBar Value="{Binding ProgressValue}" Width="200" Height="20" Margin="20,0,0,0"
                         Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <!-- 清理项列表 -->
        <ListView Grid.Row="1" ItemsSource="{Binding CleanupItems}" Margin="0,0,0,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding Size, Converter={StaticResource FileSizeConverter}}" 
                                   VerticalAlignment="Center" Margin="10,0" FontWeight="Bold"/>
                        <Button Grid.Column="3" Content="清理" Command="{Binding CleanCommand}" Padding="8,3" Cursor="Hand"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 总计 -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1,1,0,0" Padding="10" Margin="0,5,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="可清理总大小:" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalCleanableSize, Converter={StaticResource FileSizeConverter}}" 
                           FontWeight="Bold" Foreground="#F44336" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="3" Content="🧹 一键清理选中项" Command="{Binding CleanAllCommand}" 
                Padding="20,8" HorizontalAlignment="Left" Background="#F44336" Foreground="White" FontWeight="Bold"/>
    </Grid>
</UserControl>
